# codebase/ — code thật (GĐ11–14)

Greenfield: chưa có repo. Khi qua live slice (GĐ8) và bắt đầu build (GĐ11), điền vào đây:

- Đường dẫn repo code thật (khi có), + `.ai-understanding/` do `/atlas` dựng cạnh code.
- `codebase/workers/`, `codebase/orchestrator/` — hai nhánh build song song sau khi contract freeze.
- `delivery-standards.md`, `uat-report.md`, `runbook.md`, `ops.md` khi tới GĐ11–14.

Chưa có gì để trỏ. Xem lộ trình ở `../progress/board.md`.
