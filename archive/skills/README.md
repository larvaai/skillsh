# Archive — skill tạm cất

Skill ở đây nằm ngoài `.claude/skills/` nên KHÔNG trigger. Lôi ra dùng lại: `mv archive/skills/<name> .claude/skills/` (kiểm trùng tên trước bằng `ls .claude/skills/`).

## Bộ project-folder v2 (cất 2026-07-03): seed · meet · sketch · plan · run · gate · pulse

Bản "một bộ ôm cả pipeline": 7 skill tự làm nội dung 15 GĐ trên folder project. Đã thay bằng kiến trúc 2 lớp — lớp workspace `charter/resume/progress/fanout/checkpoint` + tái dùng nguyên skill giai đoạn (`idea`, `shape`, `stack`, `skeleton`, `backlog`, `modules`, `delivery`, `uat`, `ship`, `operate`, `partner`) — nên 7 skill này trùng vai lớp giai đoạn.

Còn giá trị tham khảo khi cần cơ chế chống lệch đậm hơn: `seed/assets/constitution.md` (bản v2) có kỳ vọng K-list + dòng "So kỳ vọng" trong bức tranh, sổ `decisions.md`/`risks.md` (thước độ khả thi, đóng bằng bằng chứng), 4 điểm chốt, luật làm-lại-không-lỗi. Phần lõi đã được hấp thụ vào constitution v3 (kỳ vọng ngày 0 trong README + DoD GĐ13 đối chiếu; phiếu checkpoint = gate-là-dữ-liệu); phần sổ risks/decisions chưa mang sang — lấy từ đây nếu cần.
